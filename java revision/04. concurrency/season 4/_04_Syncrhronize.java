/*

    NOTES:
    - synchronized keyword
        - synchronized Method:
            - synchronized (lock) {...}

        - synchronized this:
            - synchronized (this) {...}

        - synchronized class:
            - synchronized (StackStaticMethods.class) {...}

        - One True method:
            -  public synchronized int pop() {...}


    - Object lock = new Object();

 */

public class _04_Syncrhronize {

}
