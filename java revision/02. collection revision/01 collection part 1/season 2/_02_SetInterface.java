/*
    NOTES:
    Intro:
        No duplicate elements
        No multiple null 
        No insertion order maintained
        No indexing to elements


    Creating a new a set
        Set<Integer> setInt = new HashSet<>();
        Set<Integer> setInt1 = new TreeSet<>(Collection c);


    Methods:
        add(elementName)
        remove(elementName)
        addAll(Collection c)
        isEmpty()
        size()
        removeAll(Collection c)
        contains(elementName)
        containsAll(Collection c)


    Iterating over a set:
        for loop (iteration)
        while loop (iteration)
        forEach loop
        stream forEach loop
        Enhanced for loop
    
 */

public class _02_SetInterface {

}
