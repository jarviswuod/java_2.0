/*

    NOTES:
    - Creating a list;
        - default way : new ArrayList<>();
        - Via collections: new ArrayList<>(Collection c);
        - via addAll(Collection c) method

    - List features:
        - Allows duplicate
        - maintains insertion order
        - Allows multiple null elements
        - just like arrays elements can be accessed via index
        - Internally uses dynamic array

    - List methods:
        - add(element)
        - addAll(Collection c)
        - remove(element)
        - removeAll(Collection c)
        - retainAll(Collection c)
        - indexof(element)/ lastIndexOf(element)
        - set(index, element)
        - get(element or index)
        - contains(element)
        - isEmpty()

    - Iterating a List:
        - basic for loop:

        - enhanced for loop:

        - for iterator:

        - while iterator:

        - forEach():

        - stream.foreach():

    - Diff btwn LinkedList vs ArrayList:
        - Internally uses doubly Linked List to store elements, internally uses dynamic array to store elements
        - Fast in data manipulation(delete, update), Not fast because elements have to be manually adjusted every time
        - , good for storage of data
        - larger in side as it stores data plus refs to both prev and next elements, relatively smaller in size compared to LinkedList

 */

public class _02_List {

}
