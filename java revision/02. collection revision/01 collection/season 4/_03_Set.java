/*

    NOTES:
    - Creating a set;
        - default way  -> new HashSet<>();
        - Via collections -> new HashSet<>(Collection c);
        - via addAll(Collection c) method

    - Set features:
        - Allows only unique elements
        - Doesnot maintain insertion order
        - only one null element allowed

    - Set methods:
        - add(element)
        - addAll(Collection c)
        - remove(element)
        - removeAll(Collection c)
        - retainAll(Collection c)
        - get(element)
        - contains(element)
        - isEmpty()


    - Iterating a Set:
        - enhanced for loop:

        - for iterator:

        - while iterator:

        - forEach():

        - stream.foreach():

 */

public class _03_Set {

}
