/*

    NOTES:
    Creation:
        Map<key, value> map = new HashMap<>();
        Map<key, Student> map2 = new HashMap<>();
        Map<key, value> map1 = new TreeMap<>(Collection c);


    Methods:
        put(key, elementName)
        putAll(Collection c)
        remove(key)
        get(key)
        removeAll(Collection c)
        containsKey(key)
        containsValue(value)
        keySet()
        values()

        Set<String> keys = studentMarks.keySet();
        Collection<Integer> values = studentMarks.values();


    Short notes:
        No duplicate keys
        Insertion order maintained


    Iterating over a Map object
        forEach loop
            map.forEach((key,value) -> Syste.out.println(key +" : " + value))
        stream forEach loop
            map.stream().forEach((key,value) -> Syste.out.println(key +" : " + value))

Map.Entry<key, value> emtry : map.entrySet()
getKey()
getValue()


 */

public class _03_MapInterface {

}
