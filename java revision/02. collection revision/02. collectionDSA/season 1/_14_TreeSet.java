/*

    NOTES:
    - Basic Set: 
        - add(element)
        - remove(element)
        - contains(element)
        - size()
        - isEmpty()
        - clear()

    - SortedSet methods:
        - first() → smallest element in sorted set
        - last() → largest element in sorted set
        - headSet(toElement) → all elements < toElement
        - tailSet(fromElement) → all elements >= fromElement
        - subSet(from, to) → all elements >= from and < to

    - NavigableSet methods:
        - pollFirst() → remove & return smallest element
        - pollLast() → remove & return largest element
        - descendingSet() → view set in reverse order
        - descendingIterator() → iterate in reverse order

    - TreeSet methods:
        - ceiling(value) → least element >= given value
        - higher(value) → least element > given value

        - floor(value) → greatest element <= given value
        - lower(value) → greatest element < given value

 */

public class _14_TreeSet {

}