/*

    NOTES:
    - forEach((key, value) -> System.out.println("Key : " + key + " Value : " + value));
    
    - Enhanced for loop
        Entry.Set<Integer> keys = maps.entrySet();

        for(Entry<Integer> key : keys) {
            System.out.println(key + " : Value " + names.getValue(key));
        }

    - 

 */

public class _17_MapIterator {

}
