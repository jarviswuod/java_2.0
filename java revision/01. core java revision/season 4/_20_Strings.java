/*

    NOTES:
    - String
    - StringBuilder
    - StringBuffer

    - String methods:
        - length()
        - toUpperCase() / toLowerCase()
        - trim()
        - indefOf()/ lastIndexOf()
        - replace(old, new)
        - String.join(char, name)
        - split(char)
        - charAt()
        - equals() / equalsIgnoreCase()
        - startsWith(chars) / endsWith(chars)
        - subString(startIndex, endIndex)
        - contains(str)
        - isEmpty()


    - StringBuffer/ StringBuilder
        - append(str)
        - insert(offset, str)
        - replace(start, end, str)
        - delete(start, end)
        - reverse()
        - toString()
        - capacity()
        - ensureCapacity(min)
        - charAt(index)/ setCharAt(index, char)
        - length()


    - In mind
        - intern()
        - toString()
        - shallow copy()
        - largeString =  null;
        - null vs empty
        - length() && capacity()

 */

public class _20_Strings {
}