/*

    NOTES:
    - String, StringBuffer, StringBuilder

    - String methods:
        - String.join(what, which)
        - split(what)
        - length()
        - toUpperCase/ toLowerCase()
        - startsWith()/ endsWith()
        - update(old, new)
        - trim()
        - isEmpty()
        - equals(str)/ equalsIgnoreCase(str)
        - contains(str)
        - indexOf(str)/ lastIndexOf(str)



    - String buffer/ builder methods
        - append()
        - reverse()
        - insert(at, with)
        - update(start, end, with)
        - remove(from, to)
        - toString
        - intern()
        - length/ capacity()
        - trimToSize()
        - setLength(0)
        
    - Gotchas
        - null vs empty
        - intern
        - setLength(0)
        - bigString = null
        - diff btwn length & capacity
        - subString(start, end) shallow copy
        - toString() creating a new String object every time


 */

public class _17_String {

}
