/*

    NOTES:
    - A group of limited options;
    - can be used in either if() statement and switch statements
    - can have constructors, fields and methods
    - contructors are private
    - can implement interfaces but cannot extend
    - Use `values()` to iterate and `name()` or `ordinal()` to get details
    - Use `==` or `.equals()` for comparison

 */

public class _30_Enum {

}
