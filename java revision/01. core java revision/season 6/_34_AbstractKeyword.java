/*

    NOTES:
    - abstract methods only exist inside an abstract class
    - abstract class can have both abstract and concrete methods
    - abstract methods can have any access modifier(private, public, default, protected)
    - abstract methods must be implemented by the first concrete classes
    - abstract classes can have constructor
    - It uses 'extends' keyword to create concrete classes
    - Only one abstract class can be extended at the same time


    - Interface
        - Interface fields are public, static and final by defaulf
        - Interface methods are public and static too
        - Interface have no constructor
        - implemented by concrete classes
        - multiple interfaces can be implemented at the same time
        - Interfaces support default and static methods

 */

public abstract class _34_AbstractKeyword {
    public _34_AbstractKeyword() {

    }
}
