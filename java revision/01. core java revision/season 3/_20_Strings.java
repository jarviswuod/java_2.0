/*
    NOTES;
    String
        length()
        charAt()
        toUpperCase/ toLowerCase
        equals()/equalsIgnoreCase()
        startWith()/ EndsWith()
        trim()
        String.join(using,which)
        contains()
        isEmpty()]
        subString()
        replace(old, new)

    StringBuilder/ StringBuffer
        append()
        toString()
        length() && capacity()
        setLength(0)
        intern
        charAt()/ setCharAt(index, what)
        capacity()/setCapacity()
        replace(start, end, what)
        insert(at, what)
        delete(start,end)


    Key
        null && empty
        toString()
        intern()
        setLength(0)
        mirror
        something=null;
        trimToSize()
        synchronized{...}






 */

public class _20_Strings {

    public static void main(String[] args) {

    }
}
